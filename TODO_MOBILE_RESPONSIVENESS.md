# TODO - Mobile Responsiveness Fix (mobile-only)

## Goal
Fix mobile experience without changing desktop/tablet behavior.

## Steps
- [x] Hide `.side-nav` on phone widths (≤ 600px) only.
- [x] Center the hero `.brand` (“Alexa Bueno”) horizontally on mobile and ensure it sits above the `.skills` pills.
- [x] Prevent `.skills` pill crowding/overlap on mobile by adjusting mobile-only sizing + spacing.
- [x] Improve mobile typography readability for `.tagline` (and any cramped hero text) using mobile-only CSS.
- [ ] Sanity check that `.availability` and `.book-call` remain visible and functional on mobile.


